# SIM800L GSM Module Configuration
# Copy this file and modify the values as needed

# GPIO Pin Configuration (Pi Zero W)
RX_PIN=13                    # GPIO for SIM800L TX -> Pi RX (Physical pin 33)
TX_PIN=12                    # GPIO for SIM800L RX <- Pi TX (Physical pin 32)

# Serial Communication
BAUDRATE=9600               # SIM800L default baud rate

# SIM Card Configuration
SIM_PIN=9438               # Your SIM card PIN (if required)

# Test Configuration
TEST_PHONE_NUMBER=+264816828893  # Replace with actual phone number for testing

# Usage:
# source sms_config.env && python3 send_sms.py